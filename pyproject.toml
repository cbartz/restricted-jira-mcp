[project]
name = "restricted-jira-mcp"
version = "0.1.0"
description = "A project-allowlisted Jira MCP server."
requires-python = ">=3.11"
dependencies = [
  "requests>=2.31",
  "mcp>=1.0",
]

[project.optional-dependencies]
test = []

[tool.pytest.ini_options]
pythonpath = ["src"]
